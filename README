To build and run DonsProxy, type:

  mvn clean install

After the build completes, run:

   java -jar target/donsproxy-<VERSION>-jar-with-dependencies.jar

which starts the GUI.

Refer to http://donsproxy.sourceforge.net for documentation.
